<header class="header">
      <div class="header__wrapper">
        {# Toggle Button #}
        <input type="checkbox" id="nav-toggle" class="header__toggle" hidden />
        <label for="nav-toggle" class="header__menu" aria-label="Toggle menu" aria-expanded="false" aria-controls="nav">
          <span class="header__menu-icon"></span>
        </label>
        <a class="logo" {% if header.logo.url %}href="{{ header.logo.url }}"{% endif %}>
          <img src="{{ header.logo.img }}" alt="{{ header.logo.alt }}" width="{{ header.logo.width }}" height="{{ header.logo.height }}" />
          <img src="{{ header.logo.textImg}}" alt="{{ header.logo.textAlt}}" width="{{ header.logo.textWidth }}" height="{{ header.logo.textHeight }}" />
        </a>
        {% include "partials/nav.njk" %}
        <div class="header__actions">
          <form class="search header__actions-search" action="/search" method="get">
            <button class="search__button" type="submit">
              <img 
                class="search__icon"
                src="{{ header.search.icon }}"
                alt="{{ header.search.alt }}"
                width="{{ header.search.width }}"
                height="{{ header.search.height }}" />
            </button>
            <input class="search__input" type="search" name="q" placeholder="{{ header.search.placeholder }}" minlength="3" required />
          </form>
          {% for button in header.buttons %}
            {% include "partials/button.njk" %}
          {% endfor %}
        </div>
      </div>
    </header>